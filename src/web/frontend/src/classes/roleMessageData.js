class RoleMessageData {
    constructor({ id, summary, description, roles, color }) {
        this.id = id;
        this.summary = summary;
        this.description = description;
        this.roles = roles;
        this.color = color;
    }
}

export default RoleMessageData;